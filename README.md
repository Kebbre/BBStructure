# BBStructure

The app reads from `Files.json` as its live data source.

Legacy input files used to build that database are archived in `archive/` (`archive/Files Cat.json`, `archive/List of files V4.json`) for reference only; the app no longer loads them.
